{{#items}}
{{#width}}
${{className}}-width: {{width}};
{{/width}}
{{#height}}
${{className}}-height: {{height}};
{{/height}}
{{/items}}

{{#items}}
.{{className}} {
	background-image: url('data:image/svg+xml;charset=utf-8{{#isBase64}};base64{{/isBase64}},{{encoded}}');
	{{#width}}
    width: ${{className}}-width;
	{{/width}}
    {{#height}}
    height: ${{className}}-height;
    {{/height}}
}

{{/items}}
{{allClasses}} {
	background-repeat: no-repeat;
}

